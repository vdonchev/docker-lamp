# =============================================================================
# DO NOT EDIT THIS FILE DIRECTLY !!!
#
# This file is version-controlled. To override or add more vhost entries,
# create a 'vhost.local.conf' in /config/apache/vhosts
# =============================================================================
<VirtualHost *:80>
    ServerName pma.test

    ProxyPreserveHost On
    ProxyPass / http://lamp_pma:80/
    ProxyPassReverse / http://lamp_pma:80/

    ErrorLog ${APACHE_LOG_DIR}/symfony_test_pma_error.log
    CustomLog ${APACHE_LOG_DIR}/symfony_test_pma_access.log combined
</VirtualHost>

<VirtualHost *:80>
	ServerName 127.0.0.1
	ServerAlias *.test localhost

    DocumentRoot /var/www

    <Directory /var/www>
        Options +Indexes +FollowSymLinks
        AllowOverride All
        FallbackResource /index.php
        Require all granted
    </Directory>

    <IfModule dir_module>
        DirectoryIndex index.php index.html
    </IfModule>

    AddDefaultCharset UTF-8

    Header set X-Environment "Development"

    ErrorLog ${APACHE_LOG_DIR}/localhost_error.log
    CustomLog ${APACHE_LOG_DIR}/localhost_access.log combined
</VirtualHost>